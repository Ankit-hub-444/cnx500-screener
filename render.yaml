services:
  - type: web
    name: cnx500-screener
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 1 --timeout 300 app:app
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
